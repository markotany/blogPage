<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tags| %>
    <li>
      <%= link_to tag.title, tag_path(tag) %>
    </li>
  <% end %>
</ul>

<%= link_to "Create a New tag", new_tag_path, class: "new_tag" %>

